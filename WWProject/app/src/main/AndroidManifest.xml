<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cookandroid.wwproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TodayActivity" android:label="오늘의 공부"/>
        <activity android:name=".ToReviewActivity" android:label="오늘 공부의 단어 공부"/>
        <activity android:name=".ToReslutActivity" android:label="오늘 공부의 단어 리스트"/>

        <activity android:name=".DayListActivity" android:label="복습할 날짜 구하기"/>
        <activity android:name=".DayReviewActivity" android:label="복습하기"/>
        <activity android:name=".DayResultActivity" android:label="복습 결과"/>

        <activity android:name=".CardGameActivity" android:label="카드게임 테스트"/>

        <activity android:name=".LockActivity" android:label="잠금화면"/>
        <receiver android:name=".LockScreenReceiver"/>
        <service android:name=".LockScreenService"/>

        <activity android:name=".AlarmActivity"
            android:theme="@android:style/Theme.DeviceDefault.Light.Dialog"/>


        <receiver android:name=".BootReceiver"
            android:enabled="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <receiver android:name=".ReceiverActivity"/>



    </application>

</manifest>